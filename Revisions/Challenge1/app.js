"use strict";

// Moutons
let indexMouton = 1;
console.log("1er mouton");

  for (let index = 2; index <= 23; index++) {
    console.log(`${index}e Mouton`);
  };

  // Php vers javaScript

  const calculerSurface = function(rayon) {
    return    Math.PI * rayon**2;
};